{
  "name": "rwc-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run:electron": "npx cypress run",
    "cy:run:chrome": "npx cypress run --browser chrome --headless",
    "cy:run:firefox": "npx cypress run --browser firefox --headless",
    "cy:run:edge": "npx cypress run --browser egde --headless"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^5.2.0",
    "faker": "^5.1.0"
  }
}
